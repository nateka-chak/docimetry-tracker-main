export const metadata = {
  title: "Shipments - CHAK Dosimetry Tracker",
  description: "View and manage all dosimetry shipments with search and filtering capabilities",
};

export default function ShipmentsLayout({ children }) {
  return children;
}
