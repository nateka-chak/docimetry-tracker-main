export const metadata = {
  title: "Dashboard - CHAK Dosimetry Tracker",
  description: "Dashboard overview for CHAK Dosimetry Tracking System with real-time statistics and recent shipments",
};

export default function DashboardLayout({ children }) {
  return children;
}
