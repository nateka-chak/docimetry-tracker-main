export const metadata = {
  title: "Dispatch Dosimetries - CHAK Dosimetry Tracker",
  description: "Dispatch dosimetry equipment to hospitals with tracking and detailed record keeping",
};

export default function DispatchLayout({ children }) {
  return children;
}
